export interface PaginationState {
  currentPage: number;
  perPage: number;
  totalPages: number;
  totalItems: number;
  // maxDisplayedPages: number;
  // startIndex: number;
  // endIndex: number;
  // visiblePages: number[];
}